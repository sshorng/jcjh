# 2026-06-07 工作日誌

## 關鍵決策與變更
- 移除首頁 (`index.html`)，造訪直接重定向至登入頁。
- 移除了各頁面導覽列上的首頁與題庫連結，並將結果頁跳轉按鈕改為返回儀表板。
- 學生限制訪問題庫 (`review.html`)，若學生試圖進入會被攔截並轉址到儀表板。
- 實作「有作答就即時更新分析資料」功能：
  - 測驗開始時立即在 `examRecords` 建立狀態為 `active` 的紀錄，並產生 `currentRecordId`。
  - 學生每次答題，使用 `updateDoc` 即時將已答答案、即時分數與用時寫入該記錄。
  - 還原草稿時一併還原 `currentRecordId`，交卷時則將狀態更新為 `completed` 並清除草稿。
  - **即時統計公式修正**：在 `dashboard.js` 的 `calculateStats` 中，將分母從試卷總題數修正為**實際已答題數**，並計入 active 的紀錄，達成「答完一題，儀表板數據隨即動態更新」。
- 解決「多班級管理」限制：
  - 在 `teacher.html` 頂部加入班級選擇下拉選單（`classSelect`）。
  - 重構 `teacher.js` 的資料加載邏輯（`loadAllClasses` / `loadStudentsInClass` / `handleClassChange`），允許教師在多班級之間切換並管理不同班級的學生成績。
- **學生名字問候與視覺對比度優化**：
  - 修正 `dashboard.js` 中 `checkAuth` 從 Firestore 的 `users` 集合載入真實的 `displayName`（如王小明），不再使用 Email 前綴作問候。
  - 強制設定歡迎橫幅內的標題與副標題文字顏色為白色（`#ffffff !important`），避免黑字在紅色背景下難以看清。
- **教師後台題庫匯入修正**：
  - 將班級管理頁面的「同步本地題庫至雲端」按鈕，改成明確的「匯入 113 年度外題庫至雲端」。
  - `teacher.js` 的匯入流程改為直接上傳 `sample-questions.json` 中 `year !== 113` 的題目與題組，並以 `setDoc` 覆寫寫入，避免只比對缺漏導致按鈕看起來沒作用。
- **教師後台題庫管理獨立化**：
  - 新增獨立的「題庫管理」區塊，將題庫匯入按鈕從班級管理移出。
  - 題庫管理頁面加入題目總數、題組總數、年度範圍與年度分布摘要，方便快速確認匯入結果。
- **學生端題庫載入修正**：
  - `dashboard.js`、`exam-engine.js`、`question-bank.js`、`wrong-book-page.js` 改為合併載入本地題庫與 Firestore 題庫，不再只選其一。
  - `exam-engine.js` 加入頁面重新聚焦時的題庫重載保險，避免學生頁在匯入前已開啟時看不到新資料。

## 完成任務
- [x] 執行 `/sync` 自動同步與備份
- [x] 首頁移除與自動跳轉 (`index.html`)
- [x] 登入後儀表板常駐導覽列（`exam.html`、`dashboard.html`、`review.html` 導覽一致化）
- [x] 題庫訪問權限防禦（限制學生、導引教師）
- [x] 只要有作答即時寫入 Firestore 更新分析資料（`exam-engine.js` 核心重構）
- [x] 教師端多班級管理切換器（`teacher.html` 與 `teacher.js`）
- [x] 修正儀表板即時答題統計公式，支援作答未交卷即時顯示正確率 (`dashboard.js`)
- [x] 顯示真實名字與優化深紅色 Banner 文字對比度
- [x] 教師後台班級管理題庫匯入按鈕改為匯入 113 年度外題庫
- [x] 教師後台新增獨立題庫管理區塊
- [x] 學生端題庫改為本地 + Firestore 合併載入

## 待辦/後續任務
- [ ] 讓夫子於實際環境測試多班級切換與作答即時同步的效果。
- [x] 執行同步備份 (/sync)。
- [ ] 讓夫子實際點擊教師後台的題庫匯入按鈕，確認 112 至 109 年度題目可正常寫入雲端。
- [ ] 讓夫子確認學生端在重新整理或切回頁面後，能讀到雲端匯入的題庫。
