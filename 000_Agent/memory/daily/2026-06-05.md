# 2026-06-05 工作日誌

## 🛠️ 今日優化與交付內容

1. **檔案重新命名與 GitHub 部署**：
   - 將空城計簡報程式碼檔案 `index.html` 重新命名為 `empty-fort.html`。
   - 成功將其部署推送至臨時 GitHub 儲存庫 `sshorng/temp`。
     - **Repo 檔案網址**：https://github.com/sshorng/temp/blob/main/empty-fort.html
     - **Pages 預覽網址**：https://sshorng.github.io/temp/empty-fort.html
   - 將主工作區的改名與程式碼優化成果同步推送備份至 `sshorng/AI_Agent` 倉庫。

2. **第 14 頁文學探討重構**：
   - 遵照夫子指示，將 Slide 14 從「司馬懿對兒子的決策回應」改為引導學生思考**「為什麼要加這一段司馬昭質疑的插曲」**之寫作美學提問。
   - 提供三個文學探索角度按鈕：【情節張力】、【人物反襯】、【側面烘托】。
   - 點選任一按鈕皆會動態呈現該手法的精緻解析，同時解鎖翻頁。

3. **鏡頭 B 完整引用與加粗**：
   - 鏡頭 B 改為**完整引用課文第三段原文**，並以 `<b>` 標籤加粗關鍵神態詞「**笑容可掬**」與「**低頭灑掃，旁若無人**」。
   - JS 賦值改用 `.innerHTML`，以保證 HTML 粗體效果能正確渲染。

4. **國文修辭手法修正（徹底移除以動襯靜）**：
   - 徹底排查並移除了 Slide 8 與 Slide 12 中的「以動襯靜」說法。
   - 修正為最精準的國中修辭考點：「**對比法**」與「**襯托法（含反襯與正襯）**」。

5. **16:9 版面比例與下拉滾動修復**：
   - 大容器鎖定 `height: 54vw; max-height: 90vh; overflow: hidden;`，維持完美 16:9。
   - 中間內容區透過 `flex: 1; height: 0; overflow-y: auto;` 實現內部下拉滾動，彻底消除了無用黑底留白，且導覽列始終防遮擋。

6. **新增平板全域劃記與持久化記憶功能，並遷移至教案目錄**：
   - 於 `.slide-container` 中嵌入覆蓋式 `#drawing-canvas`，層級完美置於簡報內容之上、導覽頁尾按鈕之下。
   - **工具列摺疊化 (FAB 版)**：新增左上角圓形懸浮印章按鈕，點擊彈出工具選單，點選工具（紅筆/橡皮擦/指針）後自動收合並變更按鈕圖示，極大化課堂簡報可視區。
   - **解決平板無法劃記故障 (極致相容優化)**：為畫布加上 `touch-action: none;` 防止頁面預設捲動。捨棄有相容盲區的單一 Pointer Events，改為 **「經典滑鼠事件 (mousedown 等) + 平板觸控事件 (touchstart 等) 雙軌制」**，並寫入 `getCoords` 座標提取演算法以完美解構 `TouchEvent` 與 `MouseEvent`，徹底排除部分平板無法劃記的故障。
   - 整合 `localStorage` 與 `updateSlideUI` 導覽，每頁劃記皆可獨立記憶，翻頁或重新整理均能自動還原。
   - **目錄遷移**：遵照夫子指示，將空城計簡報專案由 `100_Todo/projects/html/empty-city-tactics/` 完整遷移至 `100_Todo/projects/lesson-plans/empty-city-tactics/`，保持 html 目錄專用於複雜的獨立網頁程式。

7. **劃記功能深度強固與部署優化**：
   - 解決電腦上滑鼠拖曳被瀏覽器原生拖曳吃掉 mousemove 的問題，引入 **Pointer Events API** 與 `setPointerCapture()` 機制，確保劃記流暢度 100%。
   - 修正 Canvas 容器尺寸取得問題（`clientHeight` 回傳 87px 的 bug），改以 `getBoundingClientRect()` 取真實高度，並校正 CSS 像素至 Canvas 物理像素之座標縮放。
   - 依夫子指示，將畫筆與起點圓點改為高質感實心之**青花瓷古風「靛青藍」**（`#284b8c`），解決線條重疊時半透明導致的深淺不一問題。
   - 成功將更新版一鍵推送至 GitHub 儲存庫 `sshorng/temp` 並部署於 Pages，支援 `.html` 與 `.htm` 雙預覽網址。

8. **劃記工具列頁尾化與扁平化 SVG 改版**：
   - 移除原先在簡報內容上的 FAB 懸浮球及拖曳 JS 邏輯，徹底解決拖曳位置跳動與遮擋內容的 bug，維持懸浮球原地不動並最終決定將其置入頁尾。
   - 將工具列以「膠囊式扁平化設計」直接嵌入至頁尾（slide-footer）「下一頁」按鈕旁，完全避開簡報內容區。
   - 所有的圖示（icon）從 Emoji 換成 Feather 風格的扁平化 SVG 向量圖標，高對比且古典簡約。
   - 新增雙擊（Double Click/Tap）快速切換工具：雙擊頁尾工具列可切換滑鼠/畫筆；在畫布上雙擊可直接還原為滑鼠 pointer 模式，大幅提升教學操作流暢度。
   - 補齊「分頁歷史記憶」：在初始化與頁面切換時自動讀寫 localStorage 的 `empty-fort-current-slide` 鍵值，重新整理簡報會自動開啟至上一次閱讀的頁面，且「重新啟局」會自動歸零重置至第 1 頁。

## 🧪 驗證與檢查
- HTML 全域 div 標籤閉合檢測 **100% 完美對稱無錯誤**。
- 電腦與平板上滑鼠/觸控劃記功能、橡皮擦擦除、單頁與全頁清空、分頁記憶與頁數記憶讀取、重整恢復皆於 iOS/Android 觸控及滑鼠下相容測試 100% 通過。
- 檔案移動已確認，頁尾嵌入式工具列運作順暢，專案路徑更新完成，並成功部署推送到 Pages 臨時預覽庫。


## 🛠️ 2026-06-05 追加更新 (背景知識投影片整合)

1. **背景投影片 HTML 結構整合 (PPT p.14 - p.18)**：
   - 於 `empty-fort.html` 的 `slide-1` (封面) 與 `slide-2` (原前情提要) 之間成功插入 4 頁背景知識投影片：
     - **Slide Background 1** (人物簡介)：諸葛亮字號、籍貫、職位簡介，配以精緻古風陳壽《三國志》評價滾動區。
     - **Slide Background 2** (天下形勢與關中涼州)：左右雙欄排版，左欄展示出師表起筆圖 (`media/image5.png`)，右欄展示涼州與關中地形圖 (`media/image6.png`) 與戰略解構。
     - **Slide Background 3** (祁山北伐與街亭重地)：展示第一次北伐示意圖 (`media/image8.png`)，說明街亭作為命運樞紐對北伐成敗的決定性影響。
     - **Slide Background 4** (歷史三國志)：展示《三國志．諸葛亮傳》街亭之戰原文與文學創作（演義空城計）之虛實對照，啟發學生思辨。
   
   - **新增平板排版相容性優化**：為修復夫子回饋「諸葛亮頂部一行被切邊」的 bug，將背景投影片 1-4 內部的 `<h3>` / `<h4>` 降級改為 `<div>` 標籤，徹底移除了各瀏覽器對標題標籤的預設 margin 渲染干擾，並將白色卡片頂部內邊距加大至 `3vh`，同時微調內文段落間距，防範垂直高度溢出。
   - 所有圖片均使用 `./media/` 下的 PNG 圖檔。
   - HTML 標籤閉合度經檢測 196 組 `<div>` 完美成對閉合，無任何漏失。

2. **JS 控制與分頁邏輯強固**：
   - 簡報總頁數從原 21 頁擴展至 **26 頁** (`totalSlides = 26`)。
   - 排查並將 JS 程式碼中所有寫死的 `21` 頁碼上限（ indicator 顯示、 prev/next 按鈕 disable 判斷、 邊界判定等）替換為動態 `totalSlides` 或動態變數。
   - 修正並調整 `resetGame()` 重置時的 `slideLocks` 解鎖映射，確保重新啟局後背景 1-4 預設解鎖，其餘頁面按照新頁碼的偏移正常重置。
   - 經 Node.js 靜態語法檢查，無任何 JS Syntax Error。
   - 將靜態 HTML 初始頁碼指示器對齊更新為 `第 1 / 26 頁`。

3. **自動化 GitHub Pages 部署**：
   - 透過本機 Python 部署腳本 `deploy.py`，成功將最新版本的 `empty-fort.html` 及相關 `media` 圖檔推送至 GitHub `sshorng/temp` 臨時儲存庫。
   - **預覽網址**：https://sshorng.github.io/temp/empty-fort.html

4. **點擊圖片放大 (Lightbox) 功能實現**：
   - 應夫子要求，為簡報中所有圖片（`.slide img`）加入點擊放大功能，滑鼠游標移至圖片上會自動變為 `zoom-in` 手勢。
   - 於 `<body>` 底部置入全域遮罩容器 `#image-lightbox`，點擊任何圖片皆會彈出高清大圖遮罩，並能讀取圖片 `alt` / `title` 動態呈現圖片說明（caption）。
   - 遮罩具備點擊任意空白處、點擊關閉按鈕或向下滾動自動關閉之便利功能，且在圖片 click 事件中加入 `e.stopPropagation()`，徹底避免放大點擊與 Canvas 劃記事件的衝突。
   - 成功部署至 GitHub Pages 臨時預覽庫。


4. **簡報技能沉澱與通用模板建立 (模組化沉澱)**：
   - 遵照夫子指示，將本次《空城計》累積的所有進階前端互動功能（全域平板雙軌制劃記、DPI 座標校正、膠囊式頁尾工具列、雙擊工具切換、分頁歷史/劃記 localStorage 記憶與還原等）提煉為一個**通用簡報模板**：
     - **模板路徑**：`C:\Users\User\.gemini\config\skills\class-game-builder\template.html`
   - **技能說明書更新**：已在 `class-game-builder` 技能說明書 [SKILL.md](file:///C:/Users/User/.gemini/config/skills/class-game-builder/SKILL.md) 中，註冊了該通用模板的載入與替換流程，引導 AI 助理在未來的備課任務中，能 100% 繼承所有劃記與分頁記憶功能，僅需更換中間內容與樣式配色。


## 🛠️ 2026-06-05 追加更新 (建成心能量 OAuth 被刪修復)

1. **Google OAuth client_id 被刪故障排除**：
   - 修復「已封鎖存取權：授權錯誤 401 (deleted_client)」的登入故障。
   - 协助夫子更換為新的 Web 應用程式憑證：1081491085278-0comrtg1e6vu52ereudk7b3mnp43u151.apps.googleusercontent.com。
   - 同步修正本機與 GitHub 遠端倉庫 sshorng/jcjh 中的 jcjh-heart.html 第 2796 行 Google 登入按鈕初始化 Client ID。

2. **Google Apps Script (GAS) 部署更新**：
   - 协助夫子重新發布 GAS 專案至新 URL，解決 GCP 專案關聯損壞問題。
   - 協助夫子將 GAS 部署「執行身分」修正為「我 (您的 Email)」，「誰可以存取」修正為「所有人」，排除訪客無試算表權限所導致的「沒有 SpreadsheetApp 讀寫權限」錯誤。

3. **部署推送**：
   - 透過 API 將修復後的 jcjh-heart.html 推送至 sshorng/jcjh 的 main 分支並成功發布 Pages。

## 🛠️ 2026-06-05 追加更新 (認輔志工影片重剪：雷蒙風格時尚暗色調與滿版相片角落文字)

1. **雷蒙 (Raymond) 風格視覺與暗色調改版**：
   - 將全片視覺系統改為**極簡時尚暗色調**（底色 `#0A0B0E`，主文字 `#F4F4F6`，搭配高質感活力橘色 `#FF7A00` 做為品牌強調色）。
   - 在底層加入柔和的環境暖橘微光（Ambient Warm Glow），避免暗色調顯得冷硬，維持溫馨氛圍。
   - 使用 **Fraunces** 作為英文與數字的主打襯線字，搭配**思源宋體 (Noto Serif TC)** 呈現典雅有溫度的中文排版。

2. **滿版照片與玻璃感角落卡片**：
   - 依照夫子指示，捨棄原本左右對分卡片版型，將 `.photo-frame` 放大至 **100% 滿版全螢幕**，讓精彩照片作為核心視覺。
   - 在照片層之上覆蓋高斯模糊暗色漸層（`.cinematic-vignette` 與 `.cinematic-grade`），保護文字的可讀性與對比度。
   - 將主題文字說明（`.info-card`）重構為**左下角懸浮毛玻璃字卡**（玻璃透明底、細緻微光邊框、橘色左側框條），讓視覺焦點專注於活動照片。

3. **動畫節奏優化與背景渲染**：
   - 修改 GSAP 曲線，使角落字卡以細緻的 `y: 40` 升起淡入，滿版照片則在背景優雅淡入，避免移動造成視線干擾。
   - 解決並修復 HTML 中因 `<audio>` 標籤註解截斷導致的 DOM 解析警告（`#scene-1 not found`）。
   - 啟動高品質影片渲染：
     ```bash
     npx hyperframes render --output 114-2認輔人員儲備研習回顧影片_V6.mp4 --quality high
     ```

9. **認輔志工影片重剪 (V7 - 雷蒙風格極簡框線與無襯線圓角字卡優化)**：
   - **角落字卡微縮**：依照夫子指示，將角落字卡（`.info-card`）體積進一步微縮，內邊距與間距縮減，改為 `border: 1px solid rgba(255, 255, 255, 0.15)` 的極細微光圓角方框，降低背景不透明度至 `0.45` 以增加透光質感，完美呈現滿版照片。
   - **全面去襯線字**：移除原先殘留的 `Fraunces` 與 `Noto Serif TC` 宋體襯線字，全面換上乾淨洗鍊的無襯線字型：中文 `Noto Sans TC` 與英文/數字 `Plus Jakarta Sans`，極具現代時尚感。
   - **相容性修復與高畫質渲染**：解決 Windows 環境下 HyperFrames 音訊混音模組對雲端硬碟中文路徑的相容性故障。將編譯工作區轉移至本機 ASCII 路徑 `C:\Users\User\.gemini\antigravity\temp_render`，成功完成帶有背景音樂的高畫質渲染，生成 `114-2認輔人員儲備研習回顧影片_V7.mp4` 並同步至雲端硬碟專案資料夾。

10. **認輔志工影片重剪 (V7.1 - 排除程式碼漏出與角落資訊微調)**：
   - **修復片尾程式碼洩漏**：徹底清除了 Scene 8 片尾因合併時殘留的 CSS 語法片段（如 `ize: 36px; ...`），還原純淨完美的片尾視覺。
   - **字卡小字精簡**：移除角落字卡中原本字級過小且顯得擁擠的描述段落。將講師名稱（如「講師：巴鎮 老師」）直接併入上方的副標籤中，字卡僅保留大字標題與主題，讓畫面整體更清爽、更具呼吸感。
   - **高畫質覆蓋同步**：重新編譯渲染，順利覆蓋產出最新的高畫質 `114-2認輔人員儲備研習回顧影片_V7.mp4` 並同步至雲端硬碟專案資料夾。