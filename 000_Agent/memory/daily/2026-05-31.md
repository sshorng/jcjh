# 2026-05-31 運行紀錄

## 今日關鍵決策與完成任務

1. **工作區改名與 Git 初始化**：
   - 將 AI 大腦母資料夾路徑正式由舊的 `Antigravity` 遷移改名為 `AI_Agent`，更新並驗證了所有核心腳本與配置路徑。
   - 初始化本地 Git，配置 `.gitignore` 排除個人隱私與大檔，建立了 GitHub 私人備份 Repo (`https://github.com/sshorng/AI_Agent`) 並成功推送。
2. **全域權限配置優化**：
   - 針對跨電腦與多檔案讀寫痛點，直接修改 `C:\Users\sshor\.gemini\config\config.json`，對「康軒課文資料夾」、「Obsidian 筆記庫」與「AI_Agent 工作區」配置了全域雙向讀寫白名單，徹底消除了頻繁彈出授權確認框的問題。
3. **HTML 程式安全移入**：
   - 將個人的 HTML 小程式資料夾從 `工作/html` 安全搬移至 `100_Todo/projects/html`。
   - 在母資料夾 Git 中予以排除，確保這底下的 19 個小程式能繼續完美使用各自的獨立 GitHub 儲存庫進行版控。
4. **NotebookLM & Firebase 懶人包安裝與整合**：
   - **NotebookLM**：使用 `uv` 完成安裝，配置絕對路徑 `notebooklm-mcp.exe` 寫入 Antigravity 設定檔；引導使用者使用 `login --clear --force` 還原了因憑證過期導致的連線問題，並成功列出 27 本雲端筆記本。
   - **Firebase**：成功將其註冊至 `mcp_config.json`，引導使用者完成本機 `firebase login` 授權，並成功連線查詢專案列表（`ahmo-series`、`my-reading-platform`）。
5. **程式清理**：
   - 刪除了過期且不符合目前需求的 `lou-shi-ming` 測試技能，並將 `claude-video-specs` 規格庫收納至 `200_Reference` 參考資料庫以利根目錄整潔。
   - 刪除了本地 `scratch` 暫存資料夾。

## 待辦與後續追蹤
- 隨時可以針對 whitelisted 康軒課文進行教材與學習單開發。
- 當使用者需要將任何 HTML 小程式或備課教材上線時，可直接呼叫 GitHub CLI 進行 GitHub Pages 自動化發佈與 QR Code 生成。
