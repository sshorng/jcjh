# AI 分身工作日誌：2026-06-01

## 📌 今日目標
- 美化新住民工作小組多元文化走讀規劃簡報（PPTX 格式）。
- 優化個案管理系統月報表 Excel 匯出排版格式與檔案命名，並修正新個案個案來源判定。

## 🛠️ 關鍵決策與執行成果
1. **輸出格式調整**：原規劃為 Reveal.js (HTML) 簡報，但根據用戶最新回饋「還是用pptx輸出」，改用 `python-pptx` 程式庫重新編排與繪製幻燈片內容。
2. **設計系統導入**：
   - 採用 **暗色極簡風格**：底色 `#0B0F19`（深靛藍）、主強調 `#FF6B35`（暖橘）、次強調 `#00B4D8`（亮青）。
   - 字體統一使用 **微軟正黑體 (Microsoft JhengHei)** 搭配 **Segoe UI**。
3. **佈局模式實作**：
   - 包含封面、參/貳大單元過渡頁、2x2 四宮格卡片、三欄卡片、左右二分排版、四步驟時序箭頭進程圖。
4. **檔案成功輸出**：
   - 生成檔案存於 `G:\我的雲端硬碟\工作\0輔導組長\02專案計畫\新住民小組-走讀\新住民工作小組_會議研習\115多元文化走讀學習營規劃(工作小組報告1150602)-美化版.pptx`。
5. **個案管理系統（CCMS）更新與月報表自動化優化**（今日上午至中午）：
   - **後端 API 與 Clasp 部署**：更新前端核心應用邏輯 `ccms-app.js` 與 Apps Script 後端指令碼 `程式碼.js`。調整 Google Apps Script 的 clasp 部署與同步配置，更新 `.clasp.json`、`.claspignore` 及 `appsscript.json` 以確保本機與雲端環境能流暢同步。
   - **月報表 Excel 格式與填寫列修正**：修正 `表A-1` 與 `表A-2` 的寫入與清理起始列（分別改為第 6 列與第 5 列），完美避開多行合併標頭並保留 A-2 右側說明區塊。
   - **月報表極簡化檔案命名**：將月報表下載檔名自動化為 `[民國年][雙位月份]輔導教師工作成果表格.xlsx`（例如：`11504輔導教師工作成果表格.xlsx`）。
   - **新個案來源型別比對與防呆**：前端強制以 `parseInt` 比對 `isNew` 欄位以防型別不一致導致新個案比對失效；同時若新個案在系統中漏填個案來源，匯出時自動預設為 `7`（其他）以防月報表系統查核失敗。
   - **自動化部署**：將修改完成之 `ccms-app.js` 推送部署至 `sshorng/jcjh` 的 `main` 分支（GitHub Pages 前台）。
6. **分身核心記憶寫入與同步**（重要系統偏好）：
   - **Obsidian 筆記庫定位**：正式鎖定正確路徑 `G:\我的雲端硬碟\Obsidian\sshorng`，避免後續 session 混淆。
   - **Git & GAS 專案對應**：在 MEMORY.md 確立 `100_Todo/projects/html/` 下之子資料夾（如個案管理、公文公告、口語表達、暖心留言板等）為獨立 Git 倉庫，並規範了對應之 GAS 試算表與指令碼管理邏輯。
   - **檔案輸出路徑規範**：確立信件草稿（`drafts/emails/`）、公文通知草稿（`drafts/admin-docs/`）、教案（`projects/lesson-plans/`）的歸類。
   - **自建專屬資料夾授權**：明確授權 AI 可自主在 drafts 或 projects 下建立具清晰語意之子資料夾（如半成品或新類型草稿），維持根目錄潔淨。
7. **智慧閱讀書院（測驗再次挑戰系統）功能優化**（下午）：
   - **計時器維持第一次時間**：根據用戶最新需求，及格但未滿分（`highestScore < 100`）時，計時器將**不再重新跑表**，而是**維持靜止並直接沿用第一次挑戰所花的時間**。已還原 `isPassed.value` 判斷分支，確保流暢度與答題體驗最優化。
   - **答錯題目解鎖驗證**：先前答對的題目維持鎖定（綠色底色防改），先前答錯的題目維持完全解鎖，允許學生自由改選其他選項，並能直接沿用第一次挑戰所用的時間（避開 30 秒防呆卡點）成功提交。

## 📝 待辦 / 下次 Session 追蹤
- 待用戶開啟簡報確認排版，如需微調特定頁面再行調整。
- 隨時依據 Obsidian 筆記庫最新路徑，準備進行 Attention Scanner 或其他筆記與教材之同步與分析。
- 續待老師在實務下載月報表使用後的回饋，隨時微調各統計欄位。
